#ifndef STATE_H
#define STATE_H
#include<bits/stdc++.h>
using namespace std;

class State
{
    public:
        string id;
        State(string id);
        virtual ~State();

    protected:

    private:
};

#endif // STATE_H
